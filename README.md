# To run this code simply do as follows:
1. mvn install
2. mvn compile
3. mvn exec:java

# Note: You may need to install Maven to run this project. 
# Maven ensures all dependencies are installed before starting the project.